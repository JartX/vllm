--- a/lmcache/integration/vllm/vllm_v1_adapter.py
+++ b/lmcache/integration/vllm/vllm_v1_adapter.py
@@ -1776,9 +1776,13 @@
         # Cleanup if request was aborted
         if request.status == RequestStatus.FINISHED_ABORTED:
             # Notify storage backends of aborted requests
-            assert self.lmcache_engine is not None
-            sm = self.lmcache_engine.storage_manager
-            if sm is not None:
-                sm.cancel_request(request.request_id)
+            # On the scheduler-role connector lmcache_engine is None (it
+            # lives in the worker); skip cleanup there instead of asserting,
+            # which previously killed the engine whenever a client aborted
+            # or disconnected a request.
+            if self.lmcache_engine is not None:
+                sm = self.lmcache_engine.storage_manager
+                if sm is not None:
+                    sm.cancel_request(request.request_id)
 
             if self.async_loading:
